local M = {}

M.setup = function()
  -- Set barbar's options
end

-- nvim.builtin.bufferline = {
--   keymap = {
--     normal_mode = {
--       ["<S-l>"] = ":BufferNext<CR>",
--       ["<S-h>"] = ":BufferPrevious<CR>",
--     },
--   },
-- }

-- local keymap = require "keys.mappings"
-- keymap.append_to_defaults(nvim.builtin.bufferline.keymap)

return M
